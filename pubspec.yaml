name: modon_screens
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  cloud_firestore: ^2.4.0
  firebase_auth: ^3.0.1
  firebase_core: ^1.4.0
  firebase_messaging: ^10.0.5
  flutter:
    sdk: flutter
  google_fonts: ^2.1.0
  pin_code_fields: ^7.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/images/furniture1.png
    - assets/images/furniture2.jpg
    - assets/images/furniture3.png
